// function add(x,y) {
//   return x + y
// }

//上と一緒

const add = function (x, y) {
  return x + y
}

console.log(add(11, 1)) //変数名で呼び出し


//注意: Udemyでは**演算子が使えません。与えられた数字を同じ値で乗算するか、Math.pow()メソッドを使ってください
const square = function(num){

  return Math.pow(num,2)
  }
  